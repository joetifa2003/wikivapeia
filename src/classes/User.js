export default class User {
  constructor(type, name, email, country, city, number) {
    this.type = type
    this.name = name
    this.email = email
    this.country = country
    this.city = city
    this.number = number
  }
}
