export default class User {
  constructor(type, name, email, country, region) {
    this.type = type
    this.name = name
    this.email = email
    this.country = country
    this.region = region
  }
}
